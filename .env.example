DATABASE_URL=sqlite:///data/afl_engine.db
SQUIGGLE_USER_AGENT=AFLPredictor/1.0 (contact@example.com)

# Optional — enables OpenAI match briefings (falls back to rule-based if unset)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional — live AFL bookmaker odds via The Odds API (falls back to Squiggle tips / mock)
ODDS_API_KEY=
