services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: afl
      POSTGRES_PASSWORD: afl
      POSTGRES_DB: afl_engine
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
